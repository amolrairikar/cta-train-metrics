# cta-train-metrics
Analytics app to track CTA service metrics using the CTA Train Tracker API provided by CTA


## Contributing

### Running tests

#### Unit tests
`pipenv run pytest --cov=lambdas tests/unit --cov-report=term-missing -vv`
